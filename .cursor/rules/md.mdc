---
description: grok2api 项目改造边界与参考项目使用规则
alwaysApply: true
---

# 项目目标

本项目的目标是将当前兼容 OpenAI 的实现，收敛为只支持 Grok 的实现。

改造时优先处理模块职责、数据流向和状态边界，避免为了兼容旧 OpenAI 逻辑继续扩散参数、分支和适配层。

# 参考项目边界

以下目录只允许作为只读参考资料，不允许修改其中任何文件，也不允许在当前项目中直接引用、导入或依赖其中的代码：

- `E:\project\GitHub\Ember1012\grok2api\参考项目\codex2api-main`
  - 作用：当前项目的源项目参考，用于理解原始结构、历史行为和迁移来源。
- `E:\project\GitHub\Ember1012\grok2api\参考项目\sub2api-main`
  - 作用：Grok 授权、Grok 反代和相关对接问题的参考实现。

# 实现原则

- 遇到 Grok 对接问题时，优先参考 `sub2api-main` 的授权、请求转发、模型映射和错误处理思路，再在当前项目内重新实现。
- 删除或收敛 OpenAI 专用概念时，应同步检查配置、接口、前端展示、管理后台、文档和测试，避免残留双模型语义。
- 参考项目只能用于确认行为和设计取舍，不能成为运行时依赖或复制式拼接来源。