---
description: 改码时主会话只调度审查，业务改动必须经子代理；优先于 lazy 的「主会话直接改」
alwaysApply: true
---

# 子代理使用规则

**【硬约束】** 任何会改业务代码（`src/` 等实现文件）的请求：

1. **Plan**：交付物必须含「子代理分工」表；本阶段禁止创建/运行子代理，禁止主会话直接改业务代码。
2. **Agent / Build**：禁止主会话直接改业务代码；必须先按分工表用 Task/子代理改码，主会话只调度、合并与审查。
3. **优先级**：与 `lazy` / Ponytail（最短 diff、主会话直接改）冲突时，**以本规则为准**。lazy 仍约束子代理内部的实现质量（少抽象、少无关改动），不授权主会话绕过子代理。

涉及代码修改时，主会话只调度与审查；具体改码交给子代理。

### Plan 模式（只规划，不执行）

写 plan / 出实施方案时，**必须**在计划中增加「子代理分工」小节，并满足：

- 写明需要几个**执行子代理**（及是否需要 1 个审查子代理）
- 每个子代理写清：名称/职责类型、负责文件路径、改什么（一句话）
- 可按职责分组（如 UI 样式一组、业务逻辑一组），同组可多文件
- 极小改动（单文件、单处样式/文案/配置）也要写分工，通常 1 个执行子代理即可

**禁止在 Plan 模式：**

- 真正创建、启动、运行子代理（含 Task / subagent）
- 主会话直接改业务代码

Plan 交付物示例结构：

    ## 子代理分工（执行时按此创建，Plan 阶段不运行）
    | 子代理 | 职责 | 文件 | 改动摘要 |
    |---|---|---|---|
    | exec-ui-list | 列表样式 | src/views/list/index.css | border-spacing 10→16dip |
    | review | 审查 | 同上 diff | 核对卡片尺寸未变 |

### Agent / Build 模式（才创建子代理）

用户确认 plan、点 Build、或明确要求「执行 / 让子代理改」时：

- **必须**按 plan 中的「子代理分工」创建子代理执行修改
- 无 plan 分工表时：先补一版简短分工，再创建子代理
- 单文件：1 个执行子代理；多文件：可并行多个，按职责分组
- 优先读取并遵循 `.cursor/skills/subagents-build/SKILL.md`（若存在）

### 禁止行为（Agent / Build）

- 主会话直接改业务代码（调度、合并、审查说明除外）
- 有明确改码任务时只写方案不创建子代理
- 在 Plan 阶段创建/运行子代理